<view class="container">
  <view class="top">
    <image src="../images/6.png" alt=""></image>
  </view>
  <nav>
   <view class='dl' wx:for="{{array}}">
    <image src="{{item.img}}"></image>
    <text class="title">{{item.tit}}</text>
   </view>
  </nav>
  <view class="content">
    <view class="tit">
      <h2>精品佳作</h2>
      <p bindtap="bindViewTap">查看更多</p>
    </view>
    <view class="look">
      <view class="looka">
        <view class="dlc" wx:for="{{content}}">
          <image src="{{item.img}}"></image>
          <view class="hh">
            <text>{{item.title}}</text>
            <text>{{item.author}}</text>
          </view>
        </view>
      </view>
    </view>
  </view>
</view>